class Task {
  String title;
  String description;
  DateTime startTime;
  DateTime endTime;

  Task({
    required this.title,
    required this.description,
    required this.startTime,
    required this.endTime,
  });
}
